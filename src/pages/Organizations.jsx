export default function Organizations() {
    return (
      <div style={{ textAlign: "center", padding: "50px" }}>
        <h1>Organizations</h1>
        <p>Browse organizations in our network.</p>
        {/* We'll fetch organization data from the backend later */}
      </div>
    );
  }